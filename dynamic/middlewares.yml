http:
  middlewares:
    redirect-https:
      redirectScheme:
        scheme: https
        permanent: true

    hsts:
      headers:
        stsSeconds: 31536000
        stsIncludeSubdomains: true
        stsPreload: true

    gzip:
      compress: {}

    auth:
      basicAuth:
        users:
          # admin / ton_mdp   (remplace par ton hash)
          - "admin:$$apr1$$YPLNqhzL$$9vLU3qFghLRjHf6/.8zZj/"