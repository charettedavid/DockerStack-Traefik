http:
  middlewares:
    redirect-https:
      redirectScheme:
        scheme: https
        permanent: true

    hsts:
      headers:
        stsSeconds: 31536000
        stsIncludeSubdomains: true
        stsPreload: true

    gzip:
      compress: {}
